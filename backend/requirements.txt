# Backend runtime dependencies (simplified baseline API)
flask==3.1.3
flask-cors==6.0.1
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
transformers==5.5.0
# torch is a large download; installing the CPU-only build is much smaller/faster
# for local dev and demo purposes, e.g.:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
# Required for the multilingual (XLM-RoBERTa) tokenizer used for Setswana/
# code-switched routing — without these, transformers' fast-tokenizer
# conversion silently mis-detects the model's SentencePiece file as a
# tiktoken file and raises a confusing ValueError instead of loading it.
sentencepiece==0.2.2
protobuf==7.35.1
requests==2.33.0
python-dotenv==1.2.2
psutil==6.1.0
twikit==2.3.3
