# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# Database Configuration
DATABASE_URL=sqlite:///botswana_sentiment.db
# For production, use PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost/botswana_sentiment

# Social collection provider
# Supported values: brightdata, apify, twikit
SOCIAL_PROVIDER=brightdata

# Bright Data configuration
BRIGHTDATA_API_TOKEN=your-brightdata-api-token
BRIGHTDATA_COLLECTOR_URL=https://api.brightdata.com/your-collector-endpoint
BRIGHTDATA_TIMEOUT_SECONDS=60

# Apify configuration (query/search-capable)
APIFY_API_TOKEN=your-apify-api-token
APIFY_ACTOR_ID=your-apify-actor-id
APIFY_TIMEOUT_SECONDS=120
APIFY_WAIT_FOR_FINISH_SECONDS=120

# Twikit configuration (requires account credentials or cookies)
TWIKIT_USERNAME=your-x-username
TWIKIT_EMAIL=your-x-email
TWIKIT_PASSWORD=your-x-password
TWIKIT_COOKIES_PATH=data/twikit_cookies.json
TWIKIT_LANGUAGE=en-US
TWIKIT_ENABLE_UI_METRICS=true

# Setswana/code-switching language routing (see config.py) — on by default
USE_CODE_SWITCHING=true